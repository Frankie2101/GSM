<!DOCTYPE html>
<html lang="en">
<head>
    {{!
       Load necessary libraries and CSS for the page.
       Includes SweetAlert2 for notifications, Bootstrap for styling, and custom CSS.
     }}
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Production Output - Garment Simple MES</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="d-flex" id="wrapper">

    {{> layout/sidebar}}
    <div id="page-content-wrapper">
        {{> layout/header}}
        <main class="container-fluid p-4">
            <div class="d-flex align-items-center mb-4">
                <h1 class="h3 m-0"><i class="bi bi-box-seam-fill me-2"></i>Production Output</h1>
            </div>
            {{!
             Main search form for filtering production output records.
             Users can search by various criteria.
            }}
            <div class="card mb-4">
                <div class="card-body">
                    <form id="searchForm">
                        <div class="row g-3">
                            <div class="col-md-4">
                                <label for="keyword" class="form-label">General Search</label>
                                <input type="text" class="form-control" id="keyword" name="keyword" placeholder="Enter SO No, Style, Color...">
                            </div>
                            <div class="col-md-2">
                                <label for="outputDateFrom" class="form-label">Output Date From</label>
                                <input type="date" class="form-control" id="outputDateFrom" name="outputDateFrom">
                            </div>
                            <div class="col-md-2">
                                <label for="outputDateTo" class="form-label">To</label>
                                <input type="date" class="form-control" id="outputDateTo" name="outputDateTo">
                            </div>
                            <div class="col-md-4">
                                <label for="department" class="form-label">Department</label>
                                <input type="text" class="form-control" id="department" name="department" placeholder="Enter Department...">
                            </div>
                            <div class="col-md-4">
                                <label for="productionLine" class="form-label">Production Line</label>
                                <input type="text" class="form-control" id="productionLine" name="productionLine" placeholder="Enter Production Line...">
                            </div>
                            <div class="col-md-4 d-flex align-items-end justify-content-start">
                                {{#userPermissions.PRODUCTION_OUTPUT_CREATE_EDIT}}
                                   <button type="button" id="createBtn" class="btn btn-custom-gradient rounded-pill px-4 me-2"><i class="bi bi-plus-circle me-1"></i> Create</button>
                                {{/userPermissions.PRODUCTION_OUTPUT_CREATE_EDIT}}
                                {{#userPermissions.PRODUCTION_OUTPUT_VIEW}}
                                    <button type="button" id="searchBtn" class="btn btn-custom-gradient rounded-pill px-4 me-2"><i class="bi bi-search me-1"></i> Search</button>
                                {{/userPermissions.PRODUCTION_OUTPUT_VIEW}}
                                {{#userPermissions.PRODUCTION_OUTPUT_DELETE}}
                                    <button type="button" id="deleteBtn" class="btn btn-delete-gradient rounded-pill px-4"><i class="bi bi-trash me-1"></i> Delete</button>
                                {{/userPermissions.PRODUCTION_OUTPUT_DELETE}}
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            {{!
                Area to display the search results.
              }}
            <div class="card">
                <div class="card-header">Result</div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                            <tr>
                                <th style="width: 5%;" class="text-center"><input class="form-check-input" type="checkbox" id="selectAllCheckbox"></th>
                                <th style="width: 5%;">Seq</th>
                                <th style="width: 12%;">SO No</th>
                                <th style="width: 12%;">Style</th>
                                <th style="width: 10%;">Color</th>
                                <th style="width: 10%;">Department</th>
                                <th style="width: 10%;">Prod. Line</th>
                                <th style="width: 10%;">Output Date</th>
                                <th style="width: 10%;">Output Qty</th>
                                <th style="width: 10%;">Created By</th>
                                <th style="width: 6%;">Action</th>
                            </tr>
                            </thead>
                            {{!
                              The table body (tbody) will be populated dynamically by JavaScript.
                              This is the container for result rows returned from the API.
                            }}
                            <tbody id="outputTableBody">
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </main>
        {{> layout/footer}}
    </div>
</div>

{{!
  Modal dialog used for both Creating and Editing
  a production output record.
}}
<div class="modal fade" id="outputModal" tabindex="-1" aria-labelledby="outputModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <form id="outputForm">
                <div class="modal-header">
                    <h5 class="modal-title text-gradient-custom" id="outputModalLabel"></h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="productionOutputId" name="productionOutputId">
                    {{#_csrf}}
                        <input type="hidden" id="csrfToken" name="{{parameterName}}" value="{{token}}"/>
                    {{/_csrf}}

                    <div class="row g-3">
                        <div class="col-md-6">
                            <label for="saleOrderNo" class="form-label">Sale Order No <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="saleOrderNo" name="saleOrderNo" required>
                        </div>
                        <div class="col-md-6">
                            <label for="outputDate" class="form-label">Output Date <span class="text-danger">*</span></label>
                            <input type="date" class="form-control" id="outputDate" name="outputDate" required>
                        </div>

                        <div class="col-md-6">
                            <label for="style" class="form-label">Style <span class="text-danger">*</span></label>
                            <select class="form-select" id="style" name="style" required>
                                <option value="">-- Type SO No first --</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label for="color" class="form-label">Color <span class="text-danger">*</span></label>
                            <select class="form-select" id="color" name="color" required>
                                <option value="">-- Select Style first --</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label for="modalDepartment" class="form-label">Department</label>
                            <input type="text" class="form-control" id="modalDepartment" name="department" readonly>
                        </div>
                        <div class="col-md-6">
                            <label for="modalProductionLine" class="form-label">Production Line</label>
                            <input type="text" class="form-control" id="modalProductionLine" name="productionLine" readonly>
                        </div>
                        <div class="col-md-6">
                            <label for="outputQuantity" class="form-label">Output Quantity <span class="text-danger">*</span></label>
                            <input type="number" class="form-control" id="outputQuantity" name="outputQuantity" required min="1">
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary rounded-pill" data-bs-dismiss="modal">Close</button>
                    <button type="button" id="saveBtn" class="btn btn-custom-gradient rounded-pill px-4">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>

{{> layout/scripts}}
<script src="/js/production-output-list.js"></script>
</body>
</html>