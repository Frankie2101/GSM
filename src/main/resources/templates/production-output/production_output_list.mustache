<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Production Output - Garment Simple MES</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="d-flex" id="wrapper">
    {{> layout/sidebar}}
    <div id="page-content-wrapper">
        {{> layout/header}}
        <main class="container-fluid p-4">
            <div class="d-flex align-items-center mb-4">
                <h1 class="h3 m-0"><i class="bi bi-box-seam-fill me-2"></i>Production Output</h1>
            </div>

            <div class="card mb-4">
                <div class="card-body">
                    <form action="/production-outputs" method="get" id="searchForm">
                        <div class="row g-3">
                            <!-- Row 1 -->
                            <div class="col-md-4">
                                <label for="keyword" class="form-label">General Search</label>
                                <input type="text" class="form-control" id="keyword" name="keyword" placeholder="Enter SO No, Style, Color..." value="{{#keyword}}{{.}}{{/keyword}}">
                            </div>
                            <div class="col-md-2">
                                <label for="outputDateFrom" class="form-label">Output Date From</label>
                                <input type="date" class="form-control" id="outputDateFrom" name="outputDateFrom" value="{{#outputDateFrom}}{{.}}{{/outputDateFrom}}">
                            </div>
                            <div class="col-md-2">
                                <label for="outputDateTo" class="form-label">To</label>
                                <input type="date" class="form-control" id="outputDateTo" name="outputDateTo" value="{{#outputDateTo}}{{.}}{{/outputDateTo}}">
                            </div>

                            <!-- Row 2 -->
                            <div class="col-md-4">
                                <label for="department" class="form-label">Department</label>
                                <input type="text" class="form-control" id="department" name="department" placeholder="Enter Department..." value="{{#department}}{{.}}{{/department}}">
                            </div>
                            <div class="col-md-4">
                                <label for="productionLine" class="form-label">Production Line</label>
                                <input type="text" class="form-control" id="productionLine" name="productionLine" placeholder="Enter Production Line..." value="{{#productionLine}}{{.}}{{/productionLine}}">
                            </div>
                            <div class="col-md-4 d-flex align-items-end justify-content-start">
                                <button type="submit" class="btn btn-custom-gradient rounded-pill px-4 me-2"><i class="bi bi-search me-1"></i> Search</button>
                                <button type="button" id="deleteBtn" class="btn btn-delete-gradient rounded-pill px-4"><i class="bi bi-trash me-1"></i> Delete</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card">
                <div class="card-header">Result</div>
                <div class="card-body">
                    <form action="/production-outputs/delete" method="post" id="deleteForm">
                        {{#_csrf}}
                            <input type="hidden" name="{{parameterName}}" value="{{token}}"/>
                        {{/_csrf}}
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead class="table-light">
                                <tr>
                                    <th style="width: 5%;" class="text-center"><input class="form-check-input" type="checkbox" id="selectAllCheckbox"></th>
                                    <th style="width: 5%;">Seq</th>
                                    <th style="width: 12%;">SO No</th>
                                    <th style="width: 12%;">Style</th>
                                    <th style="width: 12%;">Color</th>
                                    <th style="width: 12%;">Department</th>
                                    <th style="width: 12%;">Prod. Line</th>
                                    <th style="width: 10%;">Output Date</th>
                                    <th style="width: 10%;">Output Qty</th>
                                    <th style="width: 10%;">Created By</th>
                                </tr>
                                </thead>
                                <tbody>
                                {{#outputs}}
                                    <tr>
                                        <td class="text-center align-middle"><input class="form-check-input row-checkbox" type="checkbox" name="selectedIds" value="{{productionOutputId}}"></td>
                                        <td class="p-3 align-middle">{{sequenceNumber}}</td>
                                        <td class="p-3 align-middle">{{saleOrderNo}}</td>
                                        <td class="p-3 align-middle">{{style}}</td>
                                        <td class="p-3 align-middle">{{color}}</td>
                                        <td class="p-3 align-middle">{{department}}</td>
                                        <td class="p-3 align-middle">{{productionLine}}</td>
                                        <td class="py-3 ps-2 align-middle">{{outputDate}}</td>
                                        <td class="py-3 ps-2 align-middle text-end">{{outputQuantity}}</td>
                                        <td class="p-3 align-middle">{{createdBy}}</td>
                                    </tr>
                                {{/outputs}}
                                {{^outputs}}
                                    <tr>
                                        <td colspan="10" class="text-center text-muted p-4">No production outputs found.</td>
                                    </tr>
                                {{/outputs}}
                                </tbody>
                            </table>
                        </div>
                    </form>
                </div>
            </div>
        </main>
        {{> layout/footer}}
    </div>
</div>
{{> layout/scripts}}
<!-- JavaScript cho chức năng chọn và xóa vẫn dùng chung được -->
<script src="/js/production-output-list.js"></script>
</body>
</html>

