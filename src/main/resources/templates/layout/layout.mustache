<!DOCTYPE html>
<html lang="en">

<head>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{!
      Defines a block named "pageTitle" with a default value.
      Child templates can override this block to set a custom page title.
    }}
    <title>{{$pageTitle}}Dashboard{{/pageTitle}} - Garment Simple MES</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
{{!
  These empty blocks act as placeholders. Child pages can define these blocks
  to set a flag, which is then used by the sidebar partial to apply the 'active' class.
}}
{{$isDashboardPage}}{{/isDashboardPage}}
{{$isUserPage}}{{/isUserPage}}
{{$isProductPage}}{{/isProductPage}}
{{$isBomTemplatePage}}{{/isBomTemplatePage}}
{{$isSaleOrderPage}}{{/isSaleOrderPage}}
{{$isBomPage}}{{/isBomPage}}
{{$isPurchaseOrderPage}}{{/isPurchaseOrderPage}}
{{$isProductionOutputPage}}{{/isProductionOutputPage}}

<div class="d-flex" id="wrapper">
    {{! Sidebar Menu }}
    {{> layout/sidebar}}

    {{! Content Wrapper }}
    <div id="page-content-wrapper">
        {{! Header (Navbar) }}
        {{> layout/header}}

        <main class="container-fluid p-4">
            {{!
              Defines the main content block. The content of child templates
              will be inserted here.
            }}
            {{#partial "content"}}
            {{/partial}}
        </main>

        {{!
          Includes the footer partial.
        }}
        {{> layout/footer}}
    </div>
</div>

{{!
  Includes the scripts partial.
}}
{{> layout/scripts}}
</body>

</html>
