<!DOCTYPE html>
<html lang="en">

<head>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product - Garment Simple MES</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
<div class="d-flex" id="wrapper">
    {{> layout/sidebar}}

    <div id="page-content-wrapper">
        {{> layout/header}}

        <main class="container-fluid p-4">
            <div class="d-flex align-items-center mb-4">
                <h1 class="h3 m-0"><i class="bi bi-box-seam-fill me-2"></i>Product</h1>
            </div>

            <!-- ========== KHỐI TÌM KIẾM & HÀNH ĐỘNG ========== -->
            <div class="card mb-4">
                <div class="card-body">
                    <!-- Form tìm kiếm -->
                    <form action="/products" method="get" id="searchForm">
                        <div class="row align-items-end gy-3"> {{! Thêm gy-3 để tạo khoảng cách dọc trên mobile }}

                            {{! Cột 1: Ô tìm kiếm }}
                            <div class="col-md-4">
                                <label for="keyword" class="form-label">Search</label>
                                <input type="text" class="form-control" id="keyword" name="keyword" placeholder="Enter code or name..." value="{{#keyword}}{{.}}{{/keyword}}">
                            </div>

                            {{! Cột 2: Lựa chọn Category }}
                            <div class="col-md-3">
                                <label for="category" class="form-label">Product Category</label>
                                <select class="form-select" id="category" name="category">
                                    <option value="">All</option>
                                    {{#categories}}
                                        <option value="{{categoryName}}" {{#selected}}selected{{/selected}}>{{categoryName}}</option>
                                    {{/categories}}
                                </select>
                            </div>

                            {{! Cột 3: Các nút hành động }}
                            <div class="col-md-5 d-flex justify-content-start justify-content-md-end">
                                <button type="submit" class="btn btn-custom-gradient rounded-pill px-4 me-2"><i class="bi bi-search me-1"></i> Search</button>
                                <a href="/products/form" class="btn btn-custom-gradient rounded-pill px-4 me-2"><i class="bi bi-plus-circle me-1"></i> Create</a>
                                <button type="button" id="deleteBtn" class="btn btn-delete-gradient rounded-pill px-4"><i class="bi bi-trash me-1"></i> Delete</button>                            </div>

                        </div>
                    </form>
                </div>
            </div>

            <!-- ========== KHỐI HIỂN THỊ KẾT QUẢ ========== -->
            <div class="card">
                <div class="card-header">
                    Result
                </div>
                <div class="card-body">
                    <!-- Form để xóa sản phẩm -->
                    <form action="/products/delete" method="post" id="deleteForm">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead class="table-light">
                                <tr>
                                    <th style="width: 5%;" class="text-center"><input class="form-check-input" type="checkbox" id="selectAllCheckbox"></th>
                                    <th style="width: 10%;">Seq</th>
                                    <th style="width: 20%;">Product Code</th>
                                    <th style="width: 30%;">Product Name</th>
                                    <th style="width: 10%;">Unit</th>
                                    <th style="width: 15%;">Product Category</th>
                                    <th style="width: 10%;">Action</th>
                                </tr>
                                </thead>
                                <tbody>
                                {{#products}}
                                    <tr>
                                        <td class="text-center align-middle"><input class="form-check-input row-checkbox" type="checkbox" name="selectedIds" value="{{productId}}"></td>
                                        <td class="p-3 align-middle">{{sequenceNumber}}</td>
                                        <td class="p-3 align-middle">{{productCode}}</td>
                                        {{! YÊU CẦU 4 (MỚI): Căn giữa các cột này }}
                                        <td class="py-3 ps-2 align-middle">{{productName}}</td>
                                        <td class="py-3 ps-2 align-middle">{{unitName}}</td>
                                        <td class="py-3 ps-2 align-middle">{{unitName}}</td>
                                        <td class="p-3 align-middle">{{categoryName}}</td>
                                        <td class="p-3 align-middle">
                                            <a href="/products/form?id={{productId}}" class="action-link">View</a>
                                        </td>
                                    </tr>
                                {{/products}}
                                {{^products}}
                                    <tr>
                                        <td colspan="7" class="text-center text-muted p-4">No products found.</td>
                                    </tr>
                                {{/products}}
                                </tbody>
                            </table>
                        </div>
                    </form>
                </div>
            </div>
        </main>

        {{> layout/footer}}
    </div>
</div>

{{> layout/scripts}}
<!-- THÊM MỚI: Link tới file JS riêng cho trang này -->
<script src="/js/product-list.js"></script>
</body>
</html>
