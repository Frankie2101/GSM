<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
<div class="d-flex" id="wrapper">
    {{> layout/sidebar}}

    <div id="page-content-wrapper">
        {{> layout/header}}

        <main class="container-fluid p-4">
            {{#successMessage}}
                    <div class="alert alert-success">{{successMessage}}</div>
            {{/successMessage}}
            {{#errorMessage}}
                    <div class="alert alert-danger">{{errorMessage}}</div>
            {{/errorMessage}}

            {{#product}}
                <div class="mb-2 d-flex align-items-center">
                    <h1 class="h5 mb-0 text-gradient-custom">
                        {{! If productId exists, it's in Edit mode }}
                        {{#productId}}Edit Product{{/productId}}
                        {{! If productId does NOT exist, it's in Create mode }}
                        {{^productId}}Create New Product{{/productId}}
                    </h1>
                </div>

                <div class="d-flex justify-content-between align-items-center mb-3">
                    <a href="/products" class="btn btn-link-custom">
                        <i class="bi bi-arrow-left me-1"></i>Back
                    </a>
                    <div>
                        {{#userPermissions.PRODUCT_CREATE_EDIT}}
                            <button type="submit" form="productForm" class="btn btn-custom-gradient rounded-pill px-4 py-2">
                                Save
                            </button>
                        {{/userPermissions.PRODUCT_CREATE_EDIT}}
                    </div>
                </div>

                <form action="/products/save" method="post" id="productForm">
                    {{!
                        This block renders a hidden input field for CSRF protection.
                        Spring Security provides the _csrf object with the necessary parameterName and token.
                    }}
                    {{#_csrf}}
                        <input type="hidden" name="{{parameterName}}" value="{{token}}"/>
                    {{/_csrf}}

                    <input type="hidden" name="productId" value="{{#productId}}{{.}}{{/productId}}">

                    <div class="card mb-4">
                        <div class="card-header text-gradient-custom">
                            <i class="bi bi-info-circle me-1"></i>
                            Product Information
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <label for="productCode" class="form-label">Product Code <span class="text-danger">*</span></label>
                                    <input type="text" class="form-control" id="productCode" name="productCode" value="{{#productCode}}{{.}}{{/productCode}}" required {{#productId}}readonly{{/productId}} maxlength="50">
                                </div>
                                <div class="col-md-8 mb-3">
                                    <label for="productName" class="form-label">Product Name <span class="text-danger">*</span></label>
                                    <input type="text" class="form-control" id="productName" name="productName" value="{{#productName}}{{.}}{{/productName}}" required maxlength="100">
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="productCategory" class="form-label">Category <span class="text-danger">*</span></label>
                                    <select class="form-select" id="productCategory" name="categoryId" required {{#productId}}disabled{{/productId}}>
                                        <option value="">-- Select a Category --</option>
                                        {{#categories}}
                                            <option value="{{id}}" {{#selected}}selected{{/selected}}>{{name}}</option>
                                        {{/categories}}
                                    </select>
                                    {{#productId}}
                                        <input type="hidden" name="categoryId" value="{{product.categoryId}}" />
                                    {{/productId}}
                                </div>

                                <div class="col-md-6 mb-3">
                                    <label for="unit" class="form-label">Unit <span class="text-danger">*</span></label>
                                    <select class="form-select" id="unit" name="unitId" required {{#productId}}disabled{{/productId}}>
                                        <option value="">-- Select a Unit --</option>
                                        {{#units}}
                                            <option value="{{id}}" {{#selected}}selected{{/selected}}>{{name}}</option>
                                        {{/units}}
                                    </select>
                                    {{#productId}}
                                        <input type="hidden" name="unitId" value="{{product.unitId}}" />
                                    {{/productId}}
                                </div>

                            </div>

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="season" class="form-label">Season</label>
                                    <input type="text" class="form-control" id="season" name="season" value="{{#season}}{{.}}{{/season}}" placeholder="e.g., Spring/Summer 2025" maxlength="50">
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="status" class="form-label">Status <span class="text-danger">*</span></label>
                                    <select class="form-select" id="status" name="status" required>
                                        {{#statuses}}
                                            <option value="{{value}}" {{#selected}}selected{{/selected}}>
                                                {{displayName}}
                                            </option>
                                        {{/statuses}}
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card mb-4">
                        <div class="card-header text-gradient-custom">
                            <i class="bi bi-list-ul me-1"></i> Product Variants
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-bordered table-hover">
                                    <thead class="table-light">
                                    <tr>
                                        <th style="width: 1%;" class="text-center align-middle">
                                            <input class="form-check-input" type="checkbox" id="selectAllCheckbox">
                                        </th>
                                        <th style="width: 3%;" class="align-middle">Color <span class="text-danger">*</span></th>
                                        <th style="width: 3%;" class="align-middle">Size (e.g., XS,S,M,L) <span class="text-danger">*</span></th>
                                        <th style="width: 3%;" class="align-middle">Price</th>
                                        <th style="width: 3%;" class="align-middle">Currency</th>
                                        <th style="width: 3%;" class="text-center align-middle">Action</th>
                                    </tr>
                                    </thead>
                                    <tbody id="variantTableBody">
                                    {{#variants}}
                                        <tr>
                                            <td class="text-center align-middle">
                                                <input class="form-check-input row-checkbox" type="checkbox" {{^deletable}}disabled{{/deletable}}>
                                            </td>
                                            <input type="hidden" data-name="productVariantId" value="{{#productVariantId}}{{.}}{{/productVariantId}}">
                                            <input type="hidden" data-name="sku" value="{{#sku}}{{.}}{{/sku}}">
                                            <td><input type="text" data-name="color" value="{{#color}}{{.}}{{/color}}" class="form-control" required {{#productVariantId}}readonly{{/productVariantId}} maxlength="50"></td>
                                            <td><input type="text" data-name="size" value="{{#size}}{{.}}{{/size}}" class="form-control" required {{#productVariantId}}readonly{{/productVariantId}} maxlength="20"></td>
                                            <td><input type="text" step="0.01" min="0" data-name="price" value="{{#price}}{{.}}{{/price}}" class="form-control"></td>
                                            <td><input type="text" data-name="currency" value="{{#currency}}{{.}}{{/currency}}" class="form-control" maxlength="3"></td>
                                            <td class="text-center align-middle">
                                                {{#userPermissions.PRODUCT_CREATE_EDIT}}
                                                    {{#deletable}}
                                                        <button type="button" class="btn btn-sm btn-outline-danger delete-row-btn"><i class="bi bi-trash"></i></button>
                                                    {{/deletable}}
                                                    {{^deletable}}
                                                        <span class="text-muted"><i class="bi bi-lock-fill"></i></span>
                                                    {{/deletable}}
                                                {{/userPermissions.PRODUCT_CREATE_EDIT}}
                                            </td>
                                        </tr>
                                    {{/variants}}
                                    </tbody>
                                </table>
                            </div>

                            {{#userPermissions.PRODUCT_CREATE_EDIT}}
                                <div class="mt-3">
                                    <button type="button" id="add-variant-btn" class="btn btn-custom-gradient rounded-pill px-3 py-2 me-2">
                                        <i class="bi bi-plus-circle me-1"></i> New Detail
                                    </button>
                                    <button type="button" id="delete-selected-btn" class="btn btn-delete-gradient rounded-pill px-3 py-2">
                                        <i class="bi bi-trash me-1"></i> Delete
                                    </button>
                                </div>
                            {{/userPermissions.PRODUCT_CREATE_EDIT}}
                        </div>
                    </div>
                </form>
            {{/product}}

        </main>

        {{> layout/footer}}
    </div>
</div>

{{> layout/scripts}}
</body>
</html>