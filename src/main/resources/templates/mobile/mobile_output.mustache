<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Production Input</title>
    {{!
     Meta tags for Progressive Web App (PWA) functionality.
     Allows the web app to be installed on a mobile home screen and have a native-like feel.
   }}
    <meta name="theme-color" content="#ffffff"/>
    <link rel="apple-touch-icon" href="/icons/icon-192x192.png">
    <link rel="manifest" href="/manifest.json">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>

{{!
  A full-screen loading overlay to be shown during API calls.
  It prevents user interaction while data is being processed.
}}
<div id="loading-overlay" class="d-none position-fixed top-0 start-0 w-100 h-100 bg-dark bg-opacity-50" style="z-index: 1057;">
    <div class="position-absolute top-50 start-50 translate-middle">
        <div class="spinner-border text-light" style="width: 3rem; height: 3rem;" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
    </div>
</div>

{{!
  The toast notification container, positioned at the top-right of the screen.
  Used for displaying success or error messages.
}}
<div id="toast" class="position-fixed top-0 end-0 p-3">
    <div id="toast-content" class="toast align-items-center text-white bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="d-flex">
            <div class="toast-body"></div>
            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>
</div>

<div class="container d-flex align-items-center justify-content-center min-vh-100 p-3">
    <div class="card shadow-sm border-0 rounded-4 w-100" style="max-width: 500px;">
        <div class="card-body p-4 p-md-5 position-relative">
            <div class="text-center mb-4">
                <h1 class="h3 fw-bold m-0">Production Output</h1>
            </div>
            <form id="outputForm" class="row g-3" novalidate>
                <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}" id="csrfToken"/>
                <div class="col-12">
                    <input type="text" id="saleOrderNo" placeholder="Sale Order No" required class="form-control form-control-lg">
                </div>
                <div class="col-6">
                    <select id="style" required class="form-select form-select-lg">
                        <option value="">Style</option>
                    </select>
                </div>
                <div class="col-6">
                    <select id="color" required class="form-select form-select-lg">
                        <option value="">Color</option>
                    </select>
                </div>
                <div class="col-12">
                    <input type="number" id="outputQuantity" placeholder="Output Quantity" required class="form-control form-control-lg">
                </div>
                <div class="col-6">
                    <input type="te xt" id="department" placeholder="Department" readonly class="form-control form-control-lg bg-light">
                </div>
                <div class="col-6">
                    <input type="text" id="productionLine" placeholder="Production Line" readonly class="form-control form-control-lg bg-light">
                </div>
                <div class="col-12 d-grid">
                    <!-- === THAY ĐỔI STYLE NÚT TẠI ĐÂY === -->
                    <button type="submit" class="btn btn-custom-gradient rounded-pill btn-lg">
                        <i class="bi bi-check-circle-fill me-2"></i> Submit
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="/js/mobile-output.js"></script>
</body>
</html>

