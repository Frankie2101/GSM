{{!
    This is the Mustache template for the "Pending Approval POs" list page.
    This page is client-side rendered; it acts as a shell, and the associated
    JavaScript file (`pending-approval-list.js`) is responsible for fetching
    data via API and populating the table.
}}
<!DOCTYPE html>
<html lang="en">
<head>
    {{! ... Standard head elements ... }}
    <meta charset="UTF-8">
    <title>Pending Approval POs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    {{#_csrf}}
        <meta name="_csrf" content="{{token}}">
        <meta name="_csrf_header" content="{{headerName}}">
    {{/_csrf}}
</head>
<body>
<div class="d-flex" id="wrapper">
    {{> layout/sidebar}}
    <div id="page-content-wrapper">
        {{> layout/header}}
        <main class="container-fluid p-4">
            <h1 class="h5 mb-3 text-gradient-custom"><i class="bi bi-hourglass-split me-2"></i>Pending Approval POs</h1>

            <div class="card">
                <div class="card-header">
                    Purchase Orders waiting for your approval
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead class="table-light">
                            <tr>
                                <th style="width: 5%;">Seq</th>
                                <th>PO Number</th>
                                <th>Supplier</th>
                                <th>PO Date</th>
                                <th>Arrival Date</th>
                                <th class="text-end">Total Amount</th>
                                <th>Currency</th>
                                <th>Status</th>
                                <th class="text-center" style="width: 15%;">Action</th>
                            </tr>
                            </thead>
                            {{!
                                The table body is intentionally left empty.
                                `pending-approval-list.js` will fetch data from the `/api/purchase_orders/pending`
                                endpoint and dynamically insert all `<tr>` rows here.
                            }}
                            <tbody id="pending-po-table-body">
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </main>
        {{> layout/footer}}
    </div>
</div>

{{> layout/scripts}}
<script src="/js/pending-approval-list.js"></script>

</body>
</html>