<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BOM List - Garment Simple MES</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="d-flex" id="wrapper">
    {{> layout/sidebar}}
    <div id="page-content-wrapper">
        {{> layout/header}}
        <main class="container-fluid p-4">

            <div class="d-flex justify-content-between align-items-center mb-3">
                <h1 class="h5 mb-0 text-gradient-custom"><i class="bi bi-list-ul me-2"></i>BOM List</h1>
            </div>

            <div class="card">
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead class="table-light">
                            <tr>
                                <th style="width: 5%;">Seq</th>
                                <th>Sale Order No</th>
                                <th>BOM Template Name</th>
                                <th style="width: 10%;" class="text-center">Action</th>
                            </tr>
                            </thead>
                            <tbody>
                            {{#orderBOMs}}
                                <tr>
                                    <!-- FIX: Sử dụng {{#index}}{{/index}} để đánh số thứ tự -->
                                    <td>{{#index}}{{/index}}</td>
                                    <td>{{saleOrderNo}}</td>
                                    <td>{{bomTemplateName}}</td>
                                    <td class="text-center">
                                        <a href="/order-boms/form?saleOrderId={{saleOrderId}}" class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-eye-fill"></i> View
                                        </a>
                                    </td>
                                </tr>
                            {{/orderBOMs}}
                            {{^orderBOMs}}
                                <tr>
                                    <td colspan="4" class="text-center p-4">No BOMs found.</td>
                                </tr>
                            {{/orderBOMs}}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </main>
        {{> layout/footer}}
    </div>
</div>
{{> layout/scripts}}

<!-- THÊM MỚI: Gọi đến file JavaScript -->
<script src="/js/order-bom-list.js"></script>

</body>
</html>

