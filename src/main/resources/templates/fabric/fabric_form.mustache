<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fabric - Garment Simple MES</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
<div class="d-flex" id="wrapper">
    {{> layout/sidebar}}
    <div id="page-content-wrapper">
        {{> layout/header}}
        <main class="container-fluid p-4">
            {{#successMessage}}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    {{successMessage}}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {{/successMessage}}
            {{#errorMessage}}
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    {{errorMessage}}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {{/errorMessage}}
            {{#fabric}}
                <div class="mb-2 d-flex align-items-center">
                    <h1 class="h5 mb-0 text-gradient-custom">
                        {{#fabricId}}Edit Fabric{{/fabricId}}
                        {{^fabricId}}Create New Fabric{{/fabricId}}
                    </h1>
                </div>

                <div class="d-flex justify-content-between align-items-center mb-3">
                    <a href="/fabrics" class="btn btn-link-custom"><i class="bi bi-arrow-left me-1"></i>Back</a>
                    <div>
                        <button type="submit" form="fabricForm" class="btn btn-custom-gradient rounded-pill px-4 py-2">Save</button>
                    </div>
                </div>

                <form action="/fabrics/save" method="post" id="fabricForm">
                    <input type="hidden" name="fabricId" value="{{#fabricId}}{{.}}{{/fabricId}}">

                    <!-- ========== FABRIC INFORMATION ========== -->
                    <div class="card mb-4">
                        <div class="card-header text-gradient-custom"><i class="bi bi-info-circle me-1"></i>Fabric Information</div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <label for="fabricCode" class="form-label">Fabric Code <span class="text-danger">*</span></label>
                                    <input type="text" class="form-control" id="fabricCode" name="fabricCode" value="{{#fabricCode}}{{.}}{{/fabricCode}}" required>
                                </div>
                                <div class="col-md-8 mb-3">
                                    <label for="fabricName" class="form-label">Fabric Name <span class="text-danger">*</span></label>
                                    <input type="text" class="form-control" id="fabricName" name="fabricName" value="{{#fabricName}}{{.}}{{/fabricName}}" required>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <label for="supplierId" class="form-label">Supplier <span class="text-danger">*</span></label>
                                    <select class="form-select" id="supplierId" name="supplierId" required>
                                        <option value="">-- Select a Supplier --</option>
                                        {{#suppliers}}
                                            <option value="{{id}}" {{#selected}}selected{{/selected}}>{{name}}</option>
                                        {{/suppliers}}
                                    </select>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label for="unitId" class="form-label">Unit <span class="text-danger">*</span></label>
                                    <select class="form-select" id="unitId" name="unitId" required>
                                        <option value="">-- Select a Unit --</option>
                                        {{#units}}
                                            <option value="{{id}}" {{#selected}}selected{{/selected}}>{{name}}</option>
                                        {{/units}}
                                    </select>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label for="fabricType" class="form-label">Fabric Type</label>
                                    <input type="text" class="form-control" id="fabricType" name="fabricType" value="{{#fabricType}}{{.}}{{/fabricType}}">
                                </div>
                            </div>
                            <div class="row">

                                <div class="col-md-4 mb-3">
                                    <label for="construction" class="form-label">Construction</label>
                                    <input type="text" class="form-control" id="construction" name="construction" value="{{#construction}}{{.}}{{/construction}}">
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label for="yarnCount" class="form-label">Yarn Count</label>
                                    <input type="text" class="form-control" id="yarnCount" name="yarnCount" value="{{#yarnCount}}{{.}}{{/yarnCount}}">
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label for="finishing" class="form-label">Finishing</label>
                                    <input type="text" class="form-control" id="finishing" name="finishing" value="{{#finishing}}{{.}}{{/finishing}}">
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-12 mb-3">
                                    <label for="fabricContent" class="form-label">Fabric Content</label>
                                    <textarea class="form-control" id="fabricContent" name="fabricContent" rows="2">{{#fabricContent}}{{.}}{{/fabricContent}}</textarea>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- ========== FABRIC COLORS (DETAIL) ========== -->
                    <div class="card mb-4">
                        <div class="card-header text-gradient-custom"><i class="bi bi-palette-fill me-1"></i> Fabric Colors</div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-bordered table-hover">
                                    <thead class="table-light">
                                    <tr>
                                        <th style="width: 5%;" class="text-center align-middle"><input class="form-check-input" type="checkbox" id="selectAllCheckbox"></th>
                                        <th style="width: 20%;">Color Code <span class="text-danger">*</span></th>
                                        <th style="width: 25%;">Color Name</th>
                                        <th style="width: 15%;">Width</th>
                                        <th style="width: 15%;">Net Price</th>
                                        <th style="width: 10%;">Tax %</th>
                                        <th style="width: 10%;" class="text-center">Action</th>
                                    </tr>
                                    </thead>

                                    <tbody id="fabricColorTableBody">
                                    {{#fabricColors}}
                                        <tr>
                                            <td class="text-center align-middle">
                                                <input class="form-check-input row-checkbox" type="checkbox">
                                            </td>

                                            {{! SỬA LẠI: Dùng data-name thay vì name cho tất cả các input }}
                                            <input type="hidden" data-name="fabricColorId" value="{{#fabricColorId}}{{.}}{{/fabricColorId}}">
                                            <td><input type="text" data-name="color" value="{{#color}}{{.}}{{/color}}" class="form-control" required></td>
                                            <td><input type="text" data-name="colorName" value="{{#colorName}}{{.}}{{/colorName}}" class="form-control"></td>
                                            <td><input type="text" data-name="width" value="{{#width}}{{.}}{{/width}}" class="form-control"></td>
                                            <td><input type="number" step="0.01" data-name="netPrice" value="{{#netPrice}}{{.}}{{/netPrice}}" class="form-control"></td>
                                            <td><input type="number" step="0.01" data-name="taxPercent" value="{{#taxPercent}}{{.}}{{/taxPercent}}" class="form-control"></td>

                                            <td class="text-center align-middle">
                                                <button type="button" class="btn btn-sm btn-outline-danger delete-row-btn"><i class="bi bi-trash"></i></button>
                                            </td>
                                        </tr>
                                    {{/fabricColors}}
                                    </tbody>

                                </table>
                            </div>
                            <div class="mt-3">
                                <button type="button" id="add-color-btn" class="btn btn-custom-gradient rounded-pill px-3 py-2 me-2"><i class="bi bi-plus-circle me-1"></i> New Detail</button>
                                <button type="button" id="delete-selected-btn" class="btn btn-delete-gradient rounded-pill px-3 py-2"><i class="bi bi-trash me-1"></i> Delete</button>
                            </div>
                        </div>
                    </div>
                </form>
            {{/fabric}}
        </main>
        {{> layout/footer}}
    </div>
</div>
{{> layout/scripts}}
<script src="/js/fabric-form.js"></script>
</body>
</html>
