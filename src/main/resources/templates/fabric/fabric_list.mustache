{{!
    This is the Mustache template for the Fabric list page.
    It displays a search/filter bar and a table of all fabrics.
}}
<!DOCTYPE html>
<html lang="en">
{{! Standard head elements: SweetAlert2, Bootstrap, custom CSS }}
<head>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fabric - Garment Simple MES</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="d-flex" id="wrapper">
    {{> layout/sidebar}}
    <div id="page-content-wrapper">
        {{> layout/header}}
        <main class="container-fluid p-4">
            <div class="d-flex align-items-center mb-4">
                <h1 class="h3 m-0"><i class="bi bi-rulers me-2"></i>Fabric</h1>
            </div>

            <div class="card mb-4">
                <div class="card-body">
                    {{!
                        The search form submits a GET request to the same page (/fabrics)
                        to filter the results.
                    }}
                    <form action="/fabrics" method="get" id="searchForm">
                        <div class="row align-items-end gy-3">
                            <div class="col-md-7">
                                <label for="keyword" class="form-label">Search</label>
                                {{!
                                    The 'value' attribute is pre-filled with the keyword from the model.
                                    This keeps the search term visible after the form is submitted.
                                }}
                                <input type="text" class="form-control" id="keyword" name="keyword" placeholder="Enter fabric code or name..." value="{{#keyword}}{{.}}{{/keyword}}">
                            </div>
                            <div class="col-md-5 d-flex justify-content-start justify-content-md-end">
                                <button type="submit" class="btn btn-custom-gradient rounded-pill px-4 me-2"><i class="bi bi-search me-1"></i> Search</button>
                                {{#userPermissions.FABRIC_CREATE_EDIT}}
                                    <a href="/fabrics/form" class="btn btn-custom-gradient rounded-pill px-4 me-2"><i class="bi bi-plus-circle me-1"></i> Create</a>
                                {{/userPermissions.FABRIC_CREATE_EDIT}}
                                {{#userPermissions.FABRIC_DELETE}}
                                    <button type="button" id="deleteBtn" class="btn btn-delete-gradient rounded-pill px-4"><i class="bi bi-trash me-1"></i> Delete</button>
                                {{/userPermissions.FABRIC_DELETE}}
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card">
                <div class="card-header">Result</div>
                <div class="card-body">
                    {{!
                        This form targets the /fabrics/delete endpoint and is controlled by JavaScript.
                        It will only be submitted when the user confirms the deletion.
                    }}
                    <form action="/fabrics/delete" method="post" id="deleteForm">
                        {{#_csrf}}
                            <input type="hidden" name="{{parameterName}}" value="{{token}}"/>
                        {{/_csrf}}
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead class="table-light">
                                <tr>
                                    <th style="width: 5%;" class="text-center"><input class="form-check-input" type="checkbox" id="selectAllCheckbox"></th>
                                    <th style="width: 5%;">Seq</th>
                                    <th style="width: 15%;">Fabric Code</th>
                                    <th style="width: 25%;">Fabric Name</th>
                                    <th style="width: 15%;">Fabric Type</th>
                                    <th style="width: 15%;">Supplier</th>
                                    <th style="width: 10%;">Unit</th>
                                    <th style="width: 10%;">Action</th>
                                </tr>
                                </thead>
                                <tbody>
                                {{#fabrics}}
                                    <tr>
                                        {{! The checkbox value is the fabricId, which will be submitted for deletion. }}
                                        <td class="text-center align-middle">
                                            {{#deletable}}
                                                <input class="form-check-input row-checkbox" type="checkbox" name="selectedIds" value="{{fabricId}}">
                                            {{/deletable}}
                                            {{^deletable}}
                                                <span class="disabled-checkbox-wrapper">
                                                    <input class="form-check-input" type="checkbox" disabled>
                                                </span>
                                            {{/deletable}}
                                        </td>
                                        <td class="p-3 align-middle">{{sequenceNumber}}</td>
                                        <td class="p-3 align-middle">{{fabricCode}}</td>
                                        <td class="py-3 ps-2 align-middle">{{fabricName}}</td>
                                        <td class="py-3 ps-2 align-middle">{{fabricType}}</td>
                                        <td class="py-3 ps-2 align-middle">{{supplierName}}</td>
                                        <td class="py-3 ps-2 align-middle">{{unitName}}</td>
                                        <td class="p-3 align-middle">
                                            <a href="/fabrics/form?id={{fabricId}}" class="action-link">View</a>
                                        </td>
                                    </tr>
                                {{/fabrics}}
                                {{!
                                    Inverted section: This is displayed ONLY if the 'fabrics' list is empty.
                                }}
                                {{^fabrics}}
                                    <tr>
                                        <td colspan="8" class="text-center text-muted p-4">No fabrics found. Click 'Create' to add one.</td>
                                    </tr>
                                {{/fabrics}}
                                </tbody>
                            </table>
                        </div>
                    </form>
                </div>
            </div>
        </main>
        {{> layout/footer}}
    </div>
</div>
{{> layout/scripts}}
<script src="/js/fabric-list.js"></script>
</body>
</html>
